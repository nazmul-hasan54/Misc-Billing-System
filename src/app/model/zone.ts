export interface Zone {
    id: number;
    name: string;
    nameBn: string;
    code: string;
}
export interface ZoneModel {
    id: number;
    name: string;
    nameBn: string;
    code: string;
}
